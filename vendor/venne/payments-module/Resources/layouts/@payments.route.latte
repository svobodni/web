{block #page-content}

{control pagination, config => [ variant => '@payments.route' ]}
{control itemList $control->getItemsBuilder()->orderBy('a.date', 'DESC')->getQuery()->getResult(), config => [ variant => '@payments.route' ]}
{control pagination, config => [ variant => '@payments.route' ]}
